# Backend core
fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.36
alembic==1.13.1
pydantic==2.9.2
pydantic-settings==2.5.2
apscheduler==3.10.4
python-dotenv==1.0.1
loguru==0.7.2
httpx==0.27.0

# LLM
ollama==0.2.1
anthropic==0.28.0
numpy==2.1.3

# QR code generation (for LAN mobile access in Settings)
qrcode[pil]==7.4.2

# File processing
pdfplumber==0.11.1
ebooklib==0.18
python-docx==1.1.2
chardet==5.2.0
pillow==11.0.0
qrcode==7.4.2
watchdog==5.0.3
markdown-it-py==3.0.0
PyYAML==6.0.1

# Git versioning
gitpython==3.1.43

# Desktop notifications
plyer==2.1.0

# System tray — cross-platform
pystray==0.19.5

# Windows-only platform helpers
pywin32==308; sys_platform == "win32"
winshell==0.6; sys_platform == "win32"

# Linux tray needs PyGObject (installed via system package manager, not pip):
#   Ubuntu/Debian: sudo apt install python3-gi gir1.2-appindicator3-0.1
#   Fedora:        sudo dnf install python3-gobject libappindicator-gtk3
# Fallback (headless) works without it if DISPLAY is not available.

# Voice (optional, comment out if slow install)
# openai-whisper==20231117

# YouTube transcript (optional)
yt-dlp==2026.7.4

# Dev/test
pytest==8.2.1
pytest-asyncio==0.23.7
rich==13.7.1
